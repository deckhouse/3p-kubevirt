name: Label Approved VEP PRs
# Disabled: repository CI/automation moved to GitLab.
# Kept for manual dispatch only; no automatic runs on pull_request_target.
on:
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  check-vep:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Run VEP check script
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          TARGET_PROJECT_URL: "https://github.com/orgs/kubevirt/projects/15"
        run: python automation/label-approved-veps.py
